/*
import axios from "axios"

export const getAllDocuments = () => {
    return(
        axios
        .get(URL)
        .then((response)=>{
            const {data} = response
            return data;
        })
    )
}
*/ 